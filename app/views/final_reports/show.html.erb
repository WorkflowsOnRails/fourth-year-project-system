<% content_for :banner do %>
  <%= @final_report.task.summary %>
<% end %>

<div class="row">
  <div class="col-md-7">
    <h2>Instructions</h2>
    <%= render partial: 'description.html.erb',
        locals: {final_report: @final_report} %>
  </div>
  <div class="col-md-5">
    <h2>At a Glance</h2>
    <%= render partial: 'state.html.erb',
        locals: {final_report: @final_report} %>

    <div class="spacer"></div>

    <%= render_document_task_actions @final_report %>
  </div>
</div>

<h2>Submission History</h2>
<%= render @log_events %>

<%= will_paginate @log_events %>
